# Copy to .env and fill in. NEVER commit the real .env.
# Create keys in KoinBay: Profile -> API Management -> Create API (enable Futures trade permission).
KOINBAY_API_KEY=your_api_key_here
KOINBAY_API_SECRET=your_api_secret_here
